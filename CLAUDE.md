# モデル・エージェントルーティング規則

## 基本方針

メインClaudeは司令塔として、要求整理、タスク分割、担当選択、結果統合を行う。
原則としてメインClaude自身は大規模実装を行わない。

## 実装担当

### Codex Luna

次の場合に使用する:

- 1〜2ファイル程度の軽微な修正
- 明確なバグ修正
- テスト追加
- フォーマット修正
- コメント、型、名前の修正
- 定型的なコード生成
- 仕様が完全に確定している作業

### Codex Terra

次の場合に使用する:

- 通常規模の機能実装
- 数ファイルにまたがる変更
- 既存構造に沿った実装
- 軽度から中程度のデバッグ
- テストを伴うリファクタリング

### Sonnet coder

次の場合に使用する:

- Claude側の文脈理解を維持したまま実装したい
- 要求に曖昧さが多少残る
- 複数ファイル間の意味的整合性が必要
- 実装途中で設計判断が必要
- Codexとは独立した実装案が必要

### Codex Sol

次の場合に使用する:

- TerraまたはSonnetの実装レビュー
- 難しいアルゴリズム
- 並行処理、状態管理、セキュリティ
- 原因不明の不具合
- アーキテクチャ判断
- 実装前の批判的レビュー

### Fable escalation

以下の昇格条件のどれかを満たした場合だけ使用する:

1. 同じテストが修正後も2回以上失敗した
2. TerraとSonnetの両方が解決できなかった
3. Solのレビュー後も根本原因が不明
4. 局所修正では解決せず設計変更が必要
5. 複数のサブシステムにまたがって障害が発生している
6. 要求自体に矛盾がある
7. 修正すると別の回帰障害が繰り返し発生する
8. 既存設計を破棄すべき可能性がある

Fableは原則としてコードを直接変更しない。
分析、原因特定、設計再構築、実装計画の作成のみを行う。

## 標準フロー

1. メインClaudeが要求を整理する
2. Codex Solが必要に応じて設計をレビューする
3. 実装担当をLuna、Terra、Sonnetから1つ選ぶ
4. 実装担当がコードとテストを変更する
5. 別モデルがレビューする
6. テストを実行する
7. 問題があれば同じ担当に1回だけ再修正させる
8. 再度失敗した場合はSolへ渡す
9. 昇格条件を満たした場合だけFableへ渡す
10. Fableの計画をTerraまたはSonnetが実装する

## 排他ルール

同じworktree内で複数の実装エージェントに同時編集させない。

複数案を並列実装する場合は、エージェントごとに別worktreeを使用する。

実装者とレビュアーは原則として別モデルにする。

例:

- Luna実装 → Terraレビュー
- Terra実装 → Solレビュー
- Sonnet実装 → Solレビュー
- Sol設計 → Terra実装
- Fable再設計 → SonnetまたはTerra実装