critical_link_sender:
  ros__parameters:
    joy_input_topic: /critical_link/input/joy
    heartbeat_input_topic: /critical_link/input/heartbeat
    operator_command_input_topic: /critical_link/input/operator_command
    operator_response_output_topic: /critical_link/output/operator_response
    # Format: name|Ground-PC bind IPv4|miniPC destination IPv4|UDP port
    # The Ground PC is the 10.42.0.1 gateway for the BE25/X50 ops LAN.
    # Tailscale is an independent fallback through the phone uplink.
    udp_paths:
      - "ops_lan|10.42.0.1|10.42.0.2|45100"
      - "tailscale|100.123.47.2|100.82.157.125|45101"
    # Use a stable /dev/serial/by-id path for the Ground ESP32.
    serial_device: ""
    serial_baud: 921600
    key_file: /etc/njord/critical_link.key
